{
    "$schema": "https://opencode.ai/config.json",
    "autoupdate": true,
    "mcp": {
        "MCP_DOCKER": {
            "type": "local",
            "command": [ "docker", "mcp", "gateway", "run" ],
            "enabled": true
        }
    }
}